import express from"express";import{mountWebeact,LIBNAME}from"../src/index.min.js";import path from"path";let app=express(),PORT=3e3,EXAMPLE_DIR=path.join(process.cwd(),"example");mountWebeact(app),app.get("/",(e,o)=>{o.sendFile(path.join(EXAMPLE_DIR,"index.html"))}),app.use((e,o,p)=>{console.log("Failed to serve the request: ",e.url),p()}),app.listen(PORT,()=>{console.log(LIBNAME),console.log(`Servidor corriendo en http://localhost:${PORT} (${process.cwd()})`)});